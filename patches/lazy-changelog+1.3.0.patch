diff --git a/node_modules/lazy-changelog/dist/cli.js b/node_modules/lazy-changelog/dist/cli.js
index 76df49b..d1f1d2b 100644
--- a/node_modules/lazy-changelog/dist/cli.js
+++ b/node_modules/lazy-changelog/dist/cli.js
@@ -417,12 +417,13 @@ TASK: Write a commit message based ONLY on what you see above. Do NOT make up ch
 
 RULES:
 1. Use conventional commit format: type(scope): description
-2. Types: feat, fix, docs, style, refactor, test, chore, perf, ci, build
-3. Scope is optional but helpful (e.g., feat(auth): add login)
-4. First line should be under 72 characters
-5. If changes are complex, add a blank line then bullet points
-6. Be specific - mention actual function names, files, or components
-7. Focus on WHAT changed and WHY, not HOW
+2. Make the description in Chinese
+3. Types: feat, fix, docs, style, refactor, test, chore, perf, ci, build
+4. Scope is optional but helpful (e.g., feat(auth): add login)
+5. First line should be under 72 characters
+6. If changes are complex, add a blank line then bullet points with a Chinese body explaining what changed and why
+7. Be specific - mention actual function names, files, or components
+8. Focus on WHAT changed and WHY, not HOW
 
 EXAMPLES:
 - feat(api): add user authentication endpoint
@@ -441,21 +442,22 @@ COMMITS:
 TASK: Write a changelog entry based ONLY on what you see above. Do NOT make up features - describe what the actual code does.
 
 RULES:
-1. Read the code diffs carefully to understand what was actually built
-2. Group into: Features, Bug Fixes, Improvements (skip empty sections)
-3. If diffs are provided, prefix each item with [commits] or [code] to show the source
+1. Write the changelog in English
+2. Read the code diffs carefully to understand what was actually built
+3. Group into: Features, Bug Fixes, Improvements (skip empty sections)
 4. Be specific - mention actual function names, components, or capabilities you see in the code
 5. Keep each item to one line
+6. Append the short commit hash in parentheses at the end of each item, e.g. "- Fixed login bug (abc1234)". The hash is the 7+ character prefix at the start of each commit line in the COMMITS section above.
 
 FORMAT:
-### \u2728 Features
-- [source] Description of actual feature from the code
+### Features
+- Description of actual feature from the code (short-hash)
 
-### \u{1F41B} Bug Fixes
-- [source] Description of actual fix
+### Bug Fixes
+- Description of actual fix (short-hash)
 
-### \u{1F6E0}\uFE0F Improvements
-- [source] Description of actual improvement
+### Improvements
+- Description of actual improvement (short-hash)
 
 Now write the changelog based on the commits and code above:`;
 var AIChangelogGenerator = class {
@@ -769,9 +771,10 @@ ${diffsText}
         const { createOpenAI } = await import("@ai-sdk/openai");
         const openai = createOpenAI({
           apiKey: process.env["OPENAI_API_KEY"],
+          compatibility: "strict",
           ...baseUrl && { baseURL: baseUrl }
         });
-        providerInstance = openai(model);
+        providerInstance = openai.chat(model);
         break;
       }
       case "google": {
@@ -796,7 +799,7 @@ ${diffsText}
     const result = await generateText({
       model: providerInstance,
       prompt,
-      maxOutputTokens: 1024
+      maxOutputTokens: 8192
     });
     return result.text.trim();
   }
@@ -902,9 +905,10 @@ ${truncatedDiff}`;
         const { createOpenAI } = await import("@ai-sdk/openai");
         const openai = createOpenAI({
           apiKey: process.env["OPENAI_API_KEY"],
+          compatibility: "strict",
           ...baseUrl && { baseURL: baseUrl }
         });
-        providerInstance = openai(model);
+        providerInstance = openai.chat(model);
         break;
       }
       case "google": {
@@ -929,7 +933,7 @@ ${truncatedDiff}`;
     const result = await generateText({
       model: providerInstance,
       prompt,
-      maxOutputTokens: 500
+      maxOutputTokens: 4096
     });
     return result.text.trim();
   }
diff --git a/node_modules/lazy-changelog/dist/core.js b/node_modules/lazy-changelog/dist/core.js
index 7788ce9..cafe0fe 100644
--- a/node_modules/lazy-changelog/dist/core.js
+++ b/node_modules/lazy-changelog/dist/core.js
@@ -438,12 +438,13 @@ TASK: Write a commit message based ONLY on what you see above. Do NOT make up ch
 
 RULES:
 1. Use conventional commit format: type(scope): description
-2. Types: feat, fix, docs, style, refactor, test, chore, perf, ci, build
-3. Scope is optional but helpful (e.g., feat(auth): add login)
-4. First line should be under 72 characters
-5. If changes are complex, add a blank line then bullet points
-6. Be specific - mention actual function names, files, or components
-7. Focus on WHAT changed and WHY, not HOW
+2. Make the description in Chinese
+3. Types: feat, fix, docs, style, refactor, test, chore, perf, ci, build
+4. Scope is optional but helpful (e.g., feat(auth): add login)
+5. First line should be under 72 characters
+6. If changes are complex, add a blank line then bullet points with a Chinese body explaining what changed and why
+7. Be specific - mention actual function names, files, or components
+8. Focus on WHAT changed and WHY, not HOW
 
 EXAMPLES:
 - feat(api): add user authentication endpoint
@@ -462,21 +463,22 @@ COMMITS:
 TASK: Write a changelog entry based ONLY on what you see above. Do NOT make up features - describe what the actual code does.
 
 RULES:
-1. Read the code diffs carefully to understand what was actually built
-2. Group into: Features, Bug Fixes, Improvements (skip empty sections)
-3. If diffs are provided, prefix each item with [commits] or [code] to show the source
+1. Write the changelog in English
+2. Read the code diffs carefully to understand what was actually built
+3. Group into: Features, Bug Fixes, Improvements (skip empty sections)
 4. Be specific - mention actual function names, components, or capabilities you see in the code
 5. Keep each item to one line
+6. Append the short commit hash in parentheses at the end of each item, e.g. "- Fixed login bug (abc1234)". The hash is the 7+ character prefix at the start of each commit line in the COMMITS section above.
 
 FORMAT:
-### \u2728 Features
-- [source] Description of actual feature from the code
+### Features
+- Description of actual feature from the code (short-hash)
 
-### \u{1F41B} Bug Fixes
-- [source] Description of actual fix
+### Bug Fixes
+- Description of actual fix (short-hash)
 
-### \u{1F6E0}\uFE0F Improvements
-- [source] Description of actual improvement
+### Improvements
+- Description of actual improvement (short-hash)
 
 Now write the changelog based on the commits and code above:`;
 var AIChangelogGenerator = class {
@@ -790,9 +792,10 @@ ${diffsText}
         const { createOpenAI } = await import("@ai-sdk/openai");
         const openai = createOpenAI({
           apiKey: process.env["OPENAI_API_KEY"],
+          compatibility: "strict",
           ...baseUrl && { baseURL: baseUrl }
         });
-        providerInstance = openai(model);
+        providerInstance = openai.chat(model);
         break;
       }
       case "google": {
@@ -817,7 +820,7 @@ ${diffsText}
     const result = await generateText({
       model: providerInstance,
       prompt,
-      maxOutputTokens: 1024
+      maxOutputTokens: 8192
     });
     return result.text.trim();
   }
@@ -931,9 +934,10 @@ ${truncatedDiff}`;
         const { createOpenAI } = await import("@ai-sdk/openai");
         const openai = createOpenAI({
           apiKey: process.env["OPENAI_API_KEY"],
+          compatibility: "strict",
           ...baseUrl && { baseURL: baseUrl }
         });
-        providerInstance = openai(model);
+        providerInstance = openai.chat(model);
         break;
       }
       case "google": {
@@ -958,7 +962,7 @@ ${truncatedDiff}`;
     const result = await generateText({
       model: providerInstance,
       prompt,
-      maxOutputTokens: 500
+      maxOutputTokens: 4096
     });
     return result.text.trim();
   }
diff --git a/node_modules/lazy-changelog/dist/core.mjs b/node_modules/lazy-changelog/dist/core.mjs
index eb15852..6eae619 100644
--- a/node_modules/lazy-changelog/dist/core.mjs
+++ b/node_modules/lazy-changelog/dist/core.mjs
@@ -389,12 +389,13 @@ TASK: Write a commit message based ONLY on what you see above. Do NOT make up ch
 
 RULES:
 1. Use conventional commit format: type(scope): description
-2. Types: feat, fix, docs, style, refactor, test, chore, perf, ci, build
-3. Scope is optional but helpful (e.g., feat(auth): add login)
-4. First line should be under 72 characters
-5. If changes are complex, add a blank line then bullet points
-6. Be specific - mention actual function names, files, or components
-7. Focus on WHAT changed and WHY, not HOW
+2. Make the description in Chinese
+3. Types: feat, fix, docs, style, refactor, test, chore, perf, ci, build
+4. Scope is optional but helpful (e.g., feat(auth): add login)
+5. First line should be under 72 characters
+6. If changes are complex, add a blank line then bullet points with a Chinese body explaining what changed and why
+7. Be specific - mention actual function names, files, or components
+8. Focus on WHAT changed and WHY, not HOW
 
 EXAMPLES:
 - feat(api): add user authentication endpoint
@@ -413,21 +414,22 @@ COMMITS:
 TASK: Write a changelog entry based ONLY on what you see above. Do NOT make up features - describe what the actual code does.
 
 RULES:
-1. Read the code diffs carefully to understand what was actually built
-2. Group into: Features, Bug Fixes, Improvements (skip empty sections)
-3. If diffs are provided, prefix each item with [commits] or [code] to show the source
+1. Write the changelog in English
+2. Read the code diffs carefully to understand what was actually built
+3. Group into: Features, Bug Fixes, Improvements (skip empty sections)
 4. Be specific - mention actual function names, components, or capabilities you see in the code
 5. Keep each item to one line
+6. Append the short commit hash in parentheses at the end of each item, e.g. "- Fixed login bug (abc1234)". The hash is the 7+ character prefix at the start of each commit line in the COMMITS section above.
 
 FORMAT:
-### \u2728 Features
-- [source] Description of actual feature from the code
+### Features
+- Description of actual feature from the code (short-hash)
 
-### \u{1F41B} Bug Fixes
-- [source] Description of actual fix
+### Bug Fixes
+- Description of actual fix (short-hash)
 
-### \u{1F6E0}\uFE0F Improvements
-- [source] Description of actual improvement
+### Improvements
+- Description of actual improvement (short-hash)
 
 Now write the changelog based on the commits and code above:`;
 var AIChangelogGenerator = class {
@@ -741,9 +743,10 @@ ${diffsText}
         const { createOpenAI } = await import("@ai-sdk/openai");
         const openai = createOpenAI({
           apiKey: process.env["OPENAI_API_KEY"],
+          compatibility: "strict",
           ...baseUrl && { baseURL: baseUrl }
         });
-        providerInstance = openai(model);
+        providerInstance = openai.chat(model);
         break;
       }
       case "google": {
@@ -768,7 +771,7 @@ ${diffsText}
     const result = await generateText({
       model: providerInstance,
       prompt,
-      maxOutputTokens: 1024
+      maxOutputTokens: 8192
     });
     return result.text.trim();
   }
@@ -882,9 +885,10 @@ ${truncatedDiff}`;
         const { createOpenAI } = await import("@ai-sdk/openai");
         const openai = createOpenAI({
           apiKey: process.env["OPENAI_API_KEY"],
+          compatibility: "strict",
           ...baseUrl && { baseURL: baseUrl }
         });
-        providerInstance = openai(model);
+        providerInstance = openai.chat(model);
         break;
       }
       case "google": {
@@ -909,7 +913,7 @@ ${truncatedDiff}`;
     const result = await generateText({
       model: providerInstance,
       prompt,
-      maxOutputTokens: 500
+      maxOutputTokens: 4096
     });
     return result.text.trim();
   }
diff --git a/node_modules/lazy-changelog/dist/index.js b/node_modules/lazy-changelog/dist/index.js
index 40ab281..9cf6753 100644
--- a/node_modules/lazy-changelog/dist/index.js
+++ b/node_modules/lazy-changelog/dist/index.js
@@ -442,12 +442,13 @@ TASK: Write a commit message based ONLY on what you see above. Do NOT make up ch
 
 RULES:
 1. Use conventional commit format: type(scope): description
-2. Types: feat, fix, docs, style, refactor, test, chore, perf, ci, build
-3. Scope is optional but helpful (e.g., feat(auth): add login)
-4. First line should be under 72 characters
-5. If changes are complex, add a blank line then bullet points
-6. Be specific - mention actual function names, files, or components
-7. Focus on WHAT changed and WHY, not HOW
+2. Make the description in Chinese
+3. Types: feat, fix, docs, style, refactor, test, chore, perf, ci, build
+4. Scope is optional but helpful (e.g., feat(auth): add login)
+5. First line should be under 72 characters
+6. If changes are complex, add a blank line then bullet points with a Chinese body explaining what changed and why
+7. Be specific - mention actual function names, files, or components
+8. Focus on WHAT changed and WHY, not HOW
 
 EXAMPLES:
 - feat(api): add user authentication endpoint
@@ -466,21 +467,22 @@ COMMITS:
 TASK: Write a changelog entry based ONLY on what you see above. Do NOT make up features - describe what the actual code does.
 
 RULES:
-1. Read the code diffs carefully to understand what was actually built
-2. Group into: Features, Bug Fixes, Improvements (skip empty sections)
-3. If diffs are provided, prefix each item with [commits] or [code] to show the source
+1. Write the changelog in English
+2. Read the code diffs carefully to understand what was actually built
+3. Group into: Features, Bug Fixes, Improvements (skip empty sections)
 4. Be specific - mention actual function names, components, or capabilities you see in the code
 5. Keep each item to one line
+6. Append the short commit hash in parentheses at the end of each item, e.g. "- Fixed login bug (abc1234)". The hash is the 7+ character prefix at the start of each commit line in the COMMITS section above.
 
 FORMAT:
-### \u2728 Features
-- [source] Description of actual feature from the code
+### Features
+- Description of actual feature from the code (short-hash)
 
-### \u{1F41B} Bug Fixes
-- [source] Description of actual fix
+### Bug Fixes
+- Description of actual fix (short-hash)
 
-### \u{1F6E0}\uFE0F Improvements
-- [source] Description of actual improvement
+### Improvements
+- Description of actual improvement (short-hash)
 
 Now write the changelog based on the commits and code above:`;
 var AIChangelogGenerator = class {
@@ -794,9 +796,10 @@ ${diffsText}
         const { createOpenAI } = await import("@ai-sdk/openai");
         const openai = createOpenAI({
           apiKey: process.env["OPENAI_API_KEY"],
+          compatibility: "strict",
           ...baseUrl && { baseURL: baseUrl }
         });
-        providerInstance = openai(model);
+        providerInstance = openai.chat(model);
         break;
       }
       case "google": {
@@ -821,7 +824,7 @@ ${diffsText}
     const result = await generateText({
       model: providerInstance,
       prompt,
-      maxOutputTokens: 1024
+      maxOutputTokens: 8192
     });
     return result.text.trim();
   }
@@ -935,9 +938,10 @@ ${truncatedDiff}`;
         const { createOpenAI } = await import("@ai-sdk/openai");
         const openai = createOpenAI({
           apiKey: process.env["OPENAI_API_KEY"],
+          compatibility: "strict",
           ...baseUrl && { baseURL: baseUrl }
         });
-        providerInstance = openai(model);
+        providerInstance = openai.chat(model);
         break;
       }
       case "google": {
@@ -962,7 +966,7 @@ ${truncatedDiff}`;
     const result = await generateText({
       model: providerInstance,
       prompt,
-      maxOutputTokens: 500
+      maxOutputTokens: 4096
     });
     return result.text.trim();
   }
diff --git a/node_modules/lazy-changelog/dist/index.mjs b/node_modules/lazy-changelog/dist/index.mjs
index 192b9c4..f42116c 100644
--- a/node_modules/lazy-changelog/dist/index.mjs
+++ b/node_modules/lazy-changelog/dist/index.mjs
@@ -389,12 +389,13 @@ TASK: Write a commit message based ONLY on what you see above. Do NOT make up ch
 
 RULES:
 1. Use conventional commit format: type(scope): description
-2. Types: feat, fix, docs, style, refactor, test, chore, perf, ci, build
-3. Scope is optional but helpful (e.g., feat(auth): add login)
-4. First line should be under 72 characters
-5. If changes are complex, add a blank line then bullet points
-6. Be specific - mention actual function names, files, or components
-7. Focus on WHAT changed and WHY, not HOW
+2. Make the description in Chinese
+3. Types: feat, fix, docs, style, refactor, test, chore, perf, ci, build
+4. Scope is optional but helpful (e.g., feat(auth): add login)
+5. First line should be under 72 characters
+6. If changes are complex, add a blank line then bullet points with a Chinese body explaining what changed and why
+7. Be specific - mention actual function names, files, or components
+8. Focus on WHAT changed and WHY, not HOW
 
 EXAMPLES:
 - feat(api): add user authentication endpoint
@@ -413,21 +414,22 @@ COMMITS:
 TASK: Write a changelog entry based ONLY on what you see above. Do NOT make up features - describe what the actual code does.
 
 RULES:
-1. Read the code diffs carefully to understand what was actually built
-2. Group into: Features, Bug Fixes, Improvements (skip empty sections)
-3. If diffs are provided, prefix each item with [commits] or [code] to show the source
+1. Write the changelog in English
+2. Read the code diffs carefully to understand what was actually built
+3. Group into: Features, Bug Fixes, Improvements (skip empty sections)
 4. Be specific - mention actual function names, components, or capabilities you see in the code
 5. Keep each item to one line
+6. Append the short commit hash in parentheses at the end of each item, e.g. "- Fixed login bug (abc1234)". The hash is the 7+ character prefix at the start of each commit line in the COMMITS section above.
 
 FORMAT:
-### \u2728 Features
-- [source] Description of actual feature from the code
+### Features
+- Description of actual feature from the code (short-hash)
 
-### \u{1F41B} Bug Fixes
-- [source] Description of actual fix
+### Bug Fixes
+- Description of actual fix (short-hash)
 
-### \u{1F6E0}\uFE0F Improvements
-- [source] Description of actual improvement
+### Improvements
+- Description of actual improvement (short-hash)
 
 Now write the changelog based on the commits and code above:`;
 var AIChangelogGenerator = class {
@@ -743,7 +745,7 @@ ${diffsText}
           apiKey: process.env["OPENAI_API_KEY"],
           ...baseUrl && { baseURL: baseUrl }
         });
-        providerInstance = openai(model);
+        providerInstance = openai.chat(model);
         break;
       }
       case "google": {
@@ -768,7 +770,7 @@ ${diffsText}
     const result = await generateText({
       model: providerInstance,
       prompt,
-      maxOutputTokens: 1024
+      maxOutputTokens: 8192
     });
     return result.text.trim();
   }
@@ -884,7 +886,7 @@ ${truncatedDiff}`;
           apiKey: process.env["OPENAI_API_KEY"],
           ...baseUrl && { baseURL: baseUrl }
         });
-        providerInstance = openai(model);
+        providerInstance = openai.chat(model);
         break;
       }
       case "google": {
@@ -909,7 +911,7 @@ ${truncatedDiff}`;
     const result = await generateText({
       model: providerInstance,
       prompt,
-      maxOutputTokens: 500
+      maxOutputTokens: 4096
     });
     return result.text.trim();
   }
